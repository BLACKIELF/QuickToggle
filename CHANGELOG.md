# 更新日志（Changelog）

记录每个版本的显著变化。0.0.x 阶段按功能/修复批次递增；版本号的唯一来源是仓库根目录的 `VERSION` 文件，`build.sh` 构建时自动注入 Info.plist。

## 1.0.0（未发布）

- 相对 0.0.6：重做应用列表，增加搜索、待设置／占用筛选和逐行注册状态；设置与帮助使用独立滚动面板，反馈固定显示。
- 应用行支持鼠标、键盘和辅助功能动作；增加编辑菜单与查找／关闭入口，重新打开 App 可恢复主窗口，菜单栏可直接呼出应用。
- 新增隔离预览、过滤与预览隔离检查、三种窗口尺寸回归。预览不写回当前配置、不注册全局快捷键。
- 版本统一为 1.0.0；帮助页和菜单从包元数据读取版本，构建记录源码提交。
- 自检与发行打包使用独立目录，打包脚本遵循构建目录设置并拒绝预览包；保留原有切换引擎、配置格式和快捷键。
- 增加 GitHub Actions 构建检查、PR 模板和贡献流程；本机评审材料、偏好备份和构建产物不进入源码仓库。

- 修复：ad-hoc 签名的指定要求是二进制 cdhash，每次重建都会让辅助功能授权失效并反复弹系统授权框。自动路径（fn 探测/注册）的系统授权弹窗改为每次启动最多一次；菜单「申请辅助功能权限」与设置窗授权按钮仍可随时主动请求。build.sh 检测到钥匙串中的「QuickToggle Local Development」自签名证书时改用稳定身份签名（指定要求变为证书指纹，授权跨重建保留），缺失时回退 ad-hoc 并打印提醒。
- 设置窗视觉打磨：警告/错误状态条改为带同色浅底的圆角横幅（信息状态保持透明）；辅助功能授权按钮改为完整文案「请求辅助功能授权」、不再截断；「本机占用」标记改为橙色胶囊；列表行圆角加大、行距加宽。

- Fn/Globe 数字热键：保留现有 `⌘` 数字优先级，耗尽后推荐并可录制 `fn/🌐0–9`；使用主动 `CGEventTap` 捕获按下/释放并拦截已绑定组合，复用单次物理按压门控与代际令牌，失败及授权状态纳入菜单栏诊断。
- 系统重叠提示：读取单按 Globe 动作与标准 F 键偏好，阻止 `fn/🌐+Q`、Fn 方向键及不稳定的 `fn/🌐+F1–F12`；「本机已占用」覆盖 Fn 组合，并明确提示 macOS 无法检测所有系统、应用和键盘固件冲突。
- 两次按键状态机：Carbon 热键改为同时处理按下与释放；一次物理按压只触发一次，长按重复和按键抖动不再误触发第二次恢复，松开后仍可立即再次按下；热键管理器析构时兜底移除 handler，诊断早退也不会遗留悬空回调。
- 启动边界加固：异步启动/重开使用代际令牌；超时、取消、移除绑定或后续启动会使旧回调失效，避免过期回调覆盖新会话或刷新错误状态。
- 录制器：设置窗失去焦点时自动取消录制，并为录制中/空闲状态提供可照做的 VoiceOver 提示。
- 诊断与自检：菜单栏新增只读的应用数、热键注册数和辅助功能权限摘要；补充按键门控、异步代际边界测试及菜单/配置模型冒烟；CPU/峰值 RSS 改为进程内五次采样并按时自行结束，不再依赖 `ps` 或主动终止进程；Swift/Clang 模块缓存固定写入 `build/`，受限环境下也不依赖用户级缓存目录。

## 0.0.6 - 2026-08-23

- 文档：新增 `docs/使用说明.md`、设置截图与推文素材。
- 工程：版本号收敛到 `VERSION` 单一来源，构建号自动取 git 提交数；`build.sh` 新增 `--release` 优化构建；新增 `package.sh` 一键打包 zip；本文件回填 0.0.1–0.0.5 历史。
- 本机已占用快捷键从源码硬编码改为用户可编辑列表（存 UserDefaults；老安装首次启动自动迁移 Aident ⌘1、Wi‑Fi 菜单 ⌘2、微信 ⇧⌘W 三条记录，全新安装从空表开始）；设置窗「应用内快捷键」区可直接增删占用记录，录制四重自检照常避开。
- 新增 ⌘ 数字自动分配：添加应用时若 `⌘1–9` 有空闲组合，自动分配下一个（跳过「本机已占用」列表、已有绑定和设置窗口快捷键），走既有事务性注册；全占满时回退手动录制。全新安装从 `⌘1` 依次排起，空列表有引导文案。
- 设置窗应用列表与菜单栏列表按快捷键排序显示：`⌘数字` 升序在前，其余组合按字母顺序随后，未设置垫底；「本机已占用」记录同样按其组合内联进主列表（带橙色「本机占用」标记，可直接移除），编辑表单仍在「应用内快捷键」区。仅排序显示，不改动存储顺序。
- 修复：占用记录增删后主列表内联行不刷新的问题（改为绑定或占用任一变化即重建列表）；菜单栏列表同步内联占用条目并标注「本机占用」；设置窗「应用内快捷键」区去掉与主列表重复的占用清单，只保留添加表单。
- 首次使用引导升级：空列表显示欢迎卡片（三步上手说明 + 大号「添加第一个应用」按钮直达选择器），自动分配成功后的状态提示改为可直接照做的操作说明；配合全新安装空占用表，新用户从 ⌘1 依次排起。
- 新增配置导出/导入：菜单栏「导出配置…」生成 JSON 备份（含全部绑定、设置快捷键、启用状态、占用记录与导入标记）；「导入配置…」校验格式版本、跳过本机不存在的应用、确认后整体替换并重注册热键。
- 菜单栏顶部新增只读版本行（短版本 + 构建号，运行时读 Info.plist），配合「磁盘包较新自动重启」便于确认运行包新旧。
- 文档：README 与 docs/使用说明.md 同步以上能力；版本字样维持 0.0.5，发版时随 VERSION 统一升级。

## 0.0.5 - 2026-08-20

- 新快捷键注册前逐一探测（目标 App 是否存在、轻唤内是否重复、本机已核实占用、Carbon 独占冲突），已知本机冲突不再进入录制器。
- 飞书、OffShoot、终端、活动监视器可作为可编辑绑定一键导入。
- 休眠/解锁/点菜单栏后自动重注册快捷键；磁盘上的安装包比运行中进程新时自动重启。
- 微信 ⇧⌘W（本机已验证的启动快捷键）导入为可编辑绑定。
- 设置窗应用图标统一尺寸、归一化绘制。
- 可选「登录时启动」：默认关闭，走系统 SMAppService，失败状态如实显示。

## 0.0.4 - 2026-08-20

- 可见窗口判定收紧：只有当前 Space 的在屏窗口算「已显示」，否则直接重开。
- 第二次按键在 macOS 尚未把 App 标记为前台时也能隐藏；过期会话落回全新唤起，不再吞键。
- 新增可见应用选择器（自动扫描已安装 App）、每行 `?` 快捷键帮助、更紧凑的设置窗口。

## 0.0.3 - 2026-08-19

- 修复录制按钮卡在「请按组合键」的焦点判定问题。

## 0.0.2 - 2026-08-19

- 应用恢复失败时如实上报，不再虚报成功。

## 0.0.1 - 2026-08-18

- 首发：菜单栏全局快捷键启动/隐藏/恢复工具，单文件 Swift 实现，零第三方依赖。
- 两次按键状态机、事务性热键替换（新键注册成功才替换旧键）、无辅助功能权限时降级运行。
